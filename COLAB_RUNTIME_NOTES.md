# Colab runtime notes

- Use GPU runtime.
- The adapter ZIP must be in Google Drive.
- Do not run batch stress cells for the minimal UI.
- Run the final Gradio UI cell after `e2e_generate_one()` is defined.
